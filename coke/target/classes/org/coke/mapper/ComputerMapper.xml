<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    
    <mapper namespace="org.coke.mapper.ComputerMapper"> 
    
    	<insert id="insert">
    		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
    			select seq_board.nextval from dual
    		</selectKey>
    		insert into coke_board (bno, email, btitle, bcontent, bsort, nickname, btag)
    		values (#{bno}, #{email}, #{btitle}, #{bcontent}, #{bsort}, #{nickname}, #{btag} )
    	</insert>
    	
    	<select id="getList" resultType="org.coke.domain.BoardVO">
    		<![CDATA[
    			select *
				/*+INDEX_DESC(coke_board pk_coke_board)*/
				from
				coke_board
				where bsort = '컴퓨터' order by bno desc
    		]]>
    	</select>
    	
    	<update id="update">
    		update coke_board set btite = #{btitle}, bcontent = #{bcontent}, bsort = #{bsort}, btag = #{btag} where bno = #{bno}
    	</update>
    	
    	<delete id="delete">
    		delete from coke_board where bno = #{bno}
    	</delete>
    
    	<select id="get" resultType="org.coke.domain.BoardVO">
    		select * from coke_board where bno = #{bno}
    	</select>
    
    </mapper>

